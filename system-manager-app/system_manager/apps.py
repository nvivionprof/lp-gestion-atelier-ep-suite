from django.apps import AppConfig


class SystemManagerConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'system_manager'
    verbose_name = 'System Manager — gestion des systèmes pédagogiques'
